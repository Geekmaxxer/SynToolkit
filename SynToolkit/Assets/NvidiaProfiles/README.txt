Drop trusted .nip files (NVIDIA Profile Inspector profiles) into this folder
to have them appear in the GPU tab's "Pre-configured profiles" list.

SynToolkit does not ship any profiles here by default. Only add .nip files
whose contents you understand and trust — a profile can change real NVIDIA
driver settings when applied. This file itself is ignored (only *.nip files
are listed).
